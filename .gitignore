# Git Ignore File for Unreal Engine Projects
#
# 이 파일은 자동으로 생성되는 파일, 사용자별 설정 파일, 그리고 용량이 큰 임시 파일들이
# Git 저장소에 포함되지 않도록 관리합니다.

### Unreal Engine 생성 폴더 ###
# 이 폴더들은 빌드 과정이나 에디터 실행 중에 자동으로 생성되며, 사용자마다 다를 수 있습니다.
# 저장소에 포함시키면 용량이 커지고 병합 충돌을 유발합니다.
Binaries/
Build/
DerivedDataCache/
Intermediate/
Saved/

Content/

### IDE 및 솔루션 파일 ###
# .sln 파일은 각자 .uproject 우클릭 > 'Generate Visual Studio project files'로 생성하는 것을 권장합니다.
# 이를 통해 사용자마다 다른 엔진 버전이나 플러그인으로 인한 충돌을 방지할 수 있습니다.
*.sln
*.suo
*.user
*.csproj.user
*.VC.db
*.pdb
*.code-workspace

# IDE별 임시/설정 폴더
.vs/
.vscode/
.idea/
.ionide/

# Xcode 관련 파일
*.xcodeproj/
*.xcworkspace/

### OS 및 기타 임시 파일 ###
# 운영체제에서 생성하는 시스템 파일이나 일반적인 임시 파일입니다.
.DS_Store
Thumbs.db
*.bak
*.tmp
*~

### 특정 플러그인 캐시 (예시) ###
# 프로젝트에서 사용하는 특정 플러그인이 캐시 폴더를 생성하는 경우, 여기에 추가합니다.
# 예: FMOD Studio 플러그인 캐시
# Content/FMOD/Cache/

### 버전 관리 및 로그 파일 ###
# 퍼포스 설정 파일 (.p4config) 및 로그 파일(*.log)은 저장소에서 제외합니다.
.p4config
*.log